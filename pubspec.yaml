name: social_network_flutter
description: Opensource social network

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^3.16.2
  flutter_svg: ^0.19.1
  shared_preferences: ^0.5.12+4
  http: ^0.12.2

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/

